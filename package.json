{
  "name": "integra-automation-assessment",
  "version": "1.0.0",
  "private": true,
  "description": "QA Automation Engineer assessment - Playwright + TypeScript",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "playwright test",
    "test:api": "playwright test --project=api",
    "test:ui": "playwright test --project=ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^22.11.0",
    "dotenv": "^16.4.5",
    "typescript": "^5.7.2"
  }
}
